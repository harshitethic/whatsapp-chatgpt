# Changelog

## 2026

- Fixed the missing OpenAI key placeholder mismatch so the runtime's existing validation can recognize an unconfigured key.
- Added a small configuration check runnable with `npm test`.
- Added guidance to avoid committing real API keys.
