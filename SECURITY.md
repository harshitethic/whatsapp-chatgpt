# Security

Please do not commit real API keys, session files, or other credentials to this repository.

The bot's OpenAI key placeholder is stored in `accesser.json`. Replace the placeholder locally before running the bot, and avoid committing the resulting secret.

If you believe a credential has been exposed, rotate it with the relevant provider before continuing to use the bot.
